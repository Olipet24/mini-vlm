{
  "name": "final_retrain_v2",
  "_purpose": "Final reported primary numbers, v2: winning combo from s6 (dropout=0.2, weight-decay=0.01, n-compressed-tokens=16, pool=last, lr=1e-3, word-dropout-prob=0.15) which beat every prior config on validation loss/accuracy, including the original untuned primary. 3 seeds for mean+-std. NOT re-running baseline (unchanged, already tuned in final_retrain.json) or the blind ablation (unaffected by this regularization change). NO --skip-test -- test set touched exactly once here.",
  "base": {
    "model": "primary",
    "processed-dir": "outputs/processed_full",
    "features-dir": "outputs/features_full",
    "epochs": 30,
    "batch-size": 128,
    "num-workers": 0,
    "warmup-steps": 200,
    "early-stop-patience": 5,
    "min-epochs": 8,
    "save-best": true,
    "select-metric": "val_loss",
    "dropout": 0.2,
    "weight-decay": 0.01,
    "n-compressed-tokens": 16,
    "pool": "last",
    "lr": 0.001,
    "word-dropout-prob": 0.15
  },
  "runs": [
    {"run-name": "primary_v2_seed360", "seed": 360},
    {"run-name": "primary_v2_seed361", "seed": 361},
    {"run-name": "primary_v2_seed362", "seed": 362}
  ]
}
