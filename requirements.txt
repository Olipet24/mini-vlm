# On the CPU-only dev machine, install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# On a CUDA GPU box, install as-is (default PyPI wheels are CUDA-enabled) --
# do NOT pass the /whl/cpu index there, it silently gives you CPU-only torch.
torch>=2.4
torchvision>=0.19
numpy
pillow
matplotlib
tqdm
requests